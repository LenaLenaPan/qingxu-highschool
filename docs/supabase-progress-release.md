# 学习进度 Supabase 接入（待发布）

2026-09-19：数据库已准备，网页修改尚未推送或部署。正式页面发布后沿用测试页账号。

## 已完成

- 正式进度中心和首页共用 Supabase 会话、任务进度；22 项任务。
- 每任务一行，按账号隔离；服务器校验任务和步骤，生成完成时间与版本。
- 保存时比较版本；冲突或网络失败时停止修改并提示先读取云端。
- 切回页面、重新联网和手动读取时更新。没有后台轮询或 Realtime 订阅。
- 显式导入本机与仍可访问的旧 Netlify 云端记录；仅补充缺失任务。
- 原始作答、订正页面、旧浏览器记录和旧 Netlify API 保留。
- 测试页三条模拟数据留在测试表，没有迁入正式表。

## 验证

- 本地打包、TypeScript 类型检查和 57 页链接/脚本校验通过。
- scripts/progress-sync.test.mjs：8 项前端逻辑测试通过。
- database/test-learning-progress.sql：真实数据库内验证本账号读写、跨账号隔离、匿名拒绝、步骤验证、版本冲突、完成与撤销完成；测试数据全部回滚。
- 正式表仍为 0 行；测试表保留 3 行；账号仍为 1 个。
- 安全检查只有此前已报告的 Auth 泄露密码保护未开启提醒，未发现本次新增表的 RLS 问题。

## 发布与验收

导航续改：宽屏（1280px 及以上）显示 240px 固定侧栏，其余宽度使用原生 dialog 抽屉。学科、章节可展开，当前路径精确高亮；原页面内目录不变。侧栏通过共享脚本事件显示已确认的云端完成数量，失联时不显示过期数量。独立测试页不挂正式导航。
scripts/navigation.test.mjs 新增 7 项导航逻辑测试，与原 8 项测试全部通过。检查了所有导航链接的本地目标存在。云浏览器拒绝本地预览地址，尚未做真实浏览器视觉验收；发布前需核对窄屏、宽屏、已有页内目录和打印版，不能把逻辑测试当作视觉通过。

只有用户明确说构建或推送时才发布本分支，避免提前触发 Netlify 构建。
发布后在 /progress/ 登录，选择实际已完成的一步，核对另一设备读取结果；再验证取消勾选。
本轮未进行正式页面的真实账号浏览器端到端验收，须在发布后完成；独立测试页的登录和跨设备读写此前已由用户确认。
数据库结构是向后兼容的新增项；回退网页代码不删除 Supabase 或旧 Netlify 数据。
